import { createFont } from "../createFont";

/**
 * Placeholder font for the Custom Index divider category — replace
 * public/fonts/CustomIndex/CustomIndex.ttf with your own game's font
 * (keep the family name in sync with customIndexSettings.font).
 */
export const CustomIndex = createFont({
	family: "CustomIndex",
	src: "/fonts/CustomIndex/CustomIndex.ttf",
});
