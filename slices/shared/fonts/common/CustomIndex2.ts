import { createFont } from "../createFont";

/**
 * Placeholder font for the Custom Index 2 divider category — replace
 * public/fonts/CustomIndex2/CustomIndex2.ttf with your own game's font
 * (keep the family name in sync with customIndex2Settings.font).
 */
export const CustomIndex2 = createFont({
	family: "CustomIndex2",
	src: "/fonts/CustomIndex2/CustomIndex2.ttf",
});
